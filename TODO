## Bugs / Fixes urgentes

- Errores deprecated durante el build (en producción ARM — pendiente de log para diagnóstico).

## UX / UI

- En el listado de items mostrar el typeId.
- Filtros por categorías del itemId.
- Mostrar nombre de personaje EVE en el header en vez de la wallet address (ver `app/dashboard/page.tsx` ~86-93).

## Features

- Que los usuarios puedan marcar packs como públicos → biblioteca pública consultable.
- Estudiar vías de optimización teniendo en cuenta las refinerías (ej: Heavy Refinery ahorra lotes a costa de menos silica).

## Features (futuro)

- Indicar número de facilities disponibles por tipo para calcular tiempo de proceso en paralelo.

- Solicitudes de aprovisionamiento: el sistema presenta volúmenes totales y el proveedor selecciona cuánto suministra.
- Mover los cálculos al cliente: cachear `itemMap` en el browser, ejecutar `calculate()` en un Web Worker.

## Datos / Seed

- Repasar la tabla de loot. Muchas cosas están ahí por ser builds sin receta todavía.
- Revisar etiquetas en items.
- Confirmar nombres reales de Stack Slices (typeIds 89980-89989) — esperando API de EVE Frontier.
- Confirmar nombres reales de Shell 1/2/3 (typeIds 91749, 91967, 91968) — esperando API de EVE Frontier.

## Deuda técnica / Refactor

- Utilizar el typeId del item como ID único (en lugar de ID interno).
- Revisar BD y limpiar campos obsoletos (ej: `Item.isAsteroid`, `User.password`).
- Ver si la parte de asteroides se puede eliminar. Alternativa: usar el grupo del material y mostrarlo directamente.
- Repaso general de código.
- `Blueprint.factory String` → `facilityTypeId Int?` y `Decomposition.refinery String` → `facilityTypeId Int?` (diferido).

## Infraestructura

- Repasar la copia remota. Error: "No se encontró el directorio de backups".
